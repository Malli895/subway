# Subway Ridership Prediction Project TODO

## Steps (All Complete):
1. [x] Create requirements.txt and install dependencies
2. [x] Create and run data_download.py to fetch dataset
3. [x] Run eda_preprocessing.ipynb for data cleaning/feature engineering
4. [x] Run model_training.py to train and save model
5. [x] Launch dashboard.py with `streamlit run dashboard.py`
6. [x] View README.md for details
7. [x] Added custom styling (dashboard.css)
8. [x] Added real-time future prediction feature
9. [x] Created `run.bat` for one-command setup+run (creates venv, installs deps, opens Chrome to dashboard)

---

## Cleanup Tasks (completed):
- [x] Delete `static_dashboard/` directory (old static HTML dashboard files)
- [x] Delete `subway_env/` directory (old virtual environment)
- [x] Delete `subway_env_new/` directory (new virtual environment)
- [x] Create `.gitignore` to prevent venv/static artifacts from being tracked
- [x] Update `README.md` to highlight one-command run

**Project Ready! Run `run.bat` to launch.**

Updated after each step. All features complete: ML predictions, styling, real-time mode, one-command execution.
